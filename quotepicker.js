// {author, quote}

/*const quotes = [];
const quote = document.querySelector("header #maininfo #otherstuff #quotes h1");
const author = document.querySelector("header #maininfo #otherstuff #quotes h2");

function r(min, max) {
    return Math.ceil(Math.random() * (max-min) + min);
}

const addquote = (author, quote) => {
    author = "― " + author;
    quote = `"${quote}"`;
    quotes.push({author, quote});
}

addquote("Oscar Wilde", "Be yourself; everyone else is already taken.Be yourself; everyone else is already taken.Be yourself; everyone else is already taken.Be yourself; everyone else is already taken.Be yourself; everyone else is already taken.");

console.log(quotes);

const todayspick = r(0,0);

quote.textContent = quotes[todayspick].quote;
author.textContent = quotes[todayspick].author;*/